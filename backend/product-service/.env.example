PORT=3001
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=revest
DB_PASSWORD=revest_secret
DB_NAME=product_db

# Local dev without Docker: set true to auto-create tables (skip migrations)
# Production / Docker: set false and use npm run migration:run
DB_SYNCHRONIZE=true
JWT_SECRET=revest_jwt_secret_change_in_production
AUTH_SERVICE_URL=http://localhost:3004
